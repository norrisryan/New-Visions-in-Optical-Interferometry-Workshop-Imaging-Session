#!/bin/bash
# postBuild: runs after the Binder image is built.
# The matplotlib version cap (<3.10) has been removed because the notebook
# now monkey-patches set_oiplot_defaults() to be compatible with any
# matplotlib version. No conda version pinning needed.
